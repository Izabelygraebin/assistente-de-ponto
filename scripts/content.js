// Content script — preparação para a V2.
// Será injetado em sistemas de ponto (Ahgora, Pontomais; ver "content_scripts" no manifest.json).
// Futuro: ler o saldo do banco de horas da página e enviar ao service worker:
//   chrome.runtime.sendMessage({ tipo: "saldo-lido", saldoMin: <minutos> });
// No MVP não faz nada.
